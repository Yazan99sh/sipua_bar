name: sipua_bar
description: Widget for simple switch content by selected item in bottom bar.
repository: "https://github.com/Yazan99sh/sipua_bar.git"
issue_tracker: "https://github.com/Yazan99sh/sipua_bar/issues"
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
